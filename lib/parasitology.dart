import 'package:flutter/material.dart';
class Parasitology extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Parasitology'),
        backgroundColor: Colors.green,
      ),
      );
  }
    
  
}